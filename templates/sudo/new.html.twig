{% extends 'layout.html.twig' %}

{#{% if is_granted('ROLE_ADMIN') %}#}


{% block title %}New Sudo{% endblock %}

{% block body %}
    <h1>Create new Sudo</h1>

    {{ include('sudo/_form.html.twig') }}

    <a href="{{ path('sudo_index') }}">back to list</a>
{% endblock %}
{#{% endif %}#}
