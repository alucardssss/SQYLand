{#{% if is_granted('ROLE_ADMIN') %}#}
    <form method="post" action="{{ path('sudo_delete', {'id': sudo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_method" value="DELETE">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sudo.id) }}">
        <button class="btn">Delete</button>
    </form>
{#{% endif %}#}
